```c
#sentiment-trainer.py

Load Movie Review training data 

Load Spacy Model

Add Ensemble Text Categorization to the NLP pipeline

for epoch:
  Extract batches of random samples from training data
  
  for batch:
    Update NLP model with training samples
    
Save model to a file
```



```python
Load gender dataset

Load neural network model

for book in corpus:
  for sentence in book:
    Classify sentence as positive/negative
    
    if feminine word in sentence:
      add classification to feminine count
    else:
      add neutral label to feminine count
      
    if masculine word in sentence:
      add classification to masculine count
    else:
      add neutral label to masculine count


```
